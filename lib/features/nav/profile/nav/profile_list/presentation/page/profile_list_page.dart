import 'package:flutter/material.dart';
import 'package:get/get.dart';

class ProfileListPage extends GetView {
  const ProfileListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("List"),
    );
  }
}
